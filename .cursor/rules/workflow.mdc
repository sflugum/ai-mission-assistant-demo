---
alwaysApply: true
---

WORKFLOW:

When implementing features:
1. Start with the smallest working version
2. Avoid multi-step architecture unless necessary
3. Do not plan ahead for scalability
4. Do not refactor unless explicitly asked

When modifying code:
- Change ONLY what is requested
- Do NOT rewrite entire files
- Preserve existing structure

Ambiguity handling:
- If ambiguity is LOW:
  → implement the simplest reasonable interpretation

- If ambiguity is HIGH (multiple valid directions, risk of breaking intent):
  → ask a clarifying question instead of assuming