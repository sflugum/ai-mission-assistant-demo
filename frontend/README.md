# Frontend (React + Vite + Tailwind)

## Scripts

- `npm run dev` (Vite dev server)
- `npm run build`
- `npm run preview`

## Notes

- The UI submits `{ input: string }` to `POST /analyze`.
- In dev, `vite.config.js` proxies `/analyze` to the backend at `http://localhost:3001`.

