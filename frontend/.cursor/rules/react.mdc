---
globs: ["frontend/**/*.jsx", "frontend/**/*.js"]
---

React rules:

- Use functional components only
- Use useState/useEffect only (no advanced hooks)
- No context unless explicitly requested
- Keep components small and simple

Structure:
- One component per file ONLY if necessary
- Otherwise inline components are fine
