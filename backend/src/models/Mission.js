/**
 * Normalized mission analysis returned by POST /analyze (three string lists).
 * @typedef {Object} MissionAnalysis
 * @property {string[]} actionPlan
 * @property {string[]} risks
 * @property {string[]} tools
 */

export {}
